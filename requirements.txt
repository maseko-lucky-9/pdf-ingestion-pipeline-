docling==2.15.0
pypdfium2>=4.30.0
# OCR for scanned PDFs (slice 4 #15). pytesseract needs the `tesseract`
# binary on PATH; pdf2image needs `pdftoppm` (poppler). Both are optional;
# scripts/ocr_preprocess.py imports them lazily.
pytesseract>=0.3.10
pdf2image>=1.17.0
# pymupdf>=1.24.0  # used only by benchmarks; kept installed but not a production dep
ollama>=0.1.0
tiktoken>=0.5.0
psutil>=5.9.0
reportlab>=4.0.0
fpdf2>=2.7.0
tqdm>=4.66.0
numpy>=1.24.0
# faiss-cpu removed — replaced by sqlite-vec
scikit-learn>=1.3.0
pydantic>=2.7
pyyaml>=6.0
rich>=13.0
sqlite-vec>=0.1.0
sentence-transformers>=3.0
torch>=2.2

# Phase 3 — answer layer + REST API
anthropic>=0.40.0
fastapi>=0.115
uvicorn[standard]>=0.34
structlog>=24.1
httpx>=0.27