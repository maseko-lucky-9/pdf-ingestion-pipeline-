# Development-only dependencies. Not installed in the production container.
#
# Install:
#     pip install -r requirements.txt -r requirements-dev.txt

-r requirements.txt

pytest>=8.0
pytest-asyncio>=0.24
